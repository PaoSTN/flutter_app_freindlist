name: flutter_app_freindlist
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.6.0 <4.0.0'  # แก้ไขเป็นเวอร์ชัน Dart ที่คุณใช้ (3.6.0)

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6  # ลดเวอร์ชันลงเพื่อความเข้ากันได้
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0  # ปรับเวอร์ชันให้เข้ากับ Flutter 3.27.1

flutter:
  uses-material-design: true